file(GLOB USAGE_ENVIRONMENT_SRCS *.cpp)
file(GLOB USAGE_ENVIRONMENT_HEADERS *.hh)

INCLUDE_DIRECTORIES(
${live555Includes}
)

ADD_LIBRARY( UsageEnvironment STATIC ${USAGE_ENVIRONMENT_HEADERS} ${USAGE_ENVIRONMENT_SRCS})